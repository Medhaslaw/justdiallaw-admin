import { ViewPipesPipe } from './view-pipes.pipe';

describe('ViewPipesPipe', () => {
  it('create an instance', () => {
    const pipe = new ViewPipesPipe();
    expect(pipe).toBeTruthy();
  });
});
